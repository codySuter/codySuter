// Intentionally empty: Tailwind runs via @tailwindcss/vite. This file
// also stops Vite's upward PostCSS-config search from reaching the repo
// root's postcss.config.mjs (which needs the root project's deps).
export default { plugins: [] };
